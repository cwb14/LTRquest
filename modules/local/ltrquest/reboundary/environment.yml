name: ltrquest_reboundary
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - blast
  - pyfaidx
  - numpy
  # `parasail` on Bioconda is the C library; `import parasail` needs this one.
  - parasail-python
  - pywfa
